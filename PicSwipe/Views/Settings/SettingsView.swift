import SwiftUI

struct SettingsView: View {
    var body: some View {
        ZStack {
            Color.appBackground.ignoresSafeArea()
            Text("设置").foregroundStyle(.white)
        }
    }
}
